function solution(n)
{
    var answer = 0;

    // [실행] 버튼을 누르면 출력 값을 볼 수 있습니다.
    console.log('Hello Javascript')

    return answer;
}



function solution(n)
{
	var index = [];
  index = "n";
  var answer = 0;
  	if (i = 0, i < index.length, i++){ 
      answer += index[i];
    }
    return answer;
}

